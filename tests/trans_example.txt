function main()
    x = 0;
    flag = 0;
    while 1   
        if flag == 0 then
            flag = 1;
            op = readChar();
        else
            op = readChar();
            op = readChar();
        end if

        num = readInt();

        if op == 43 then
            x = x + num;
        end if
        if op == 45 then
            x = x - num;
        end if
        if op == 42 then
            x = x * num;
        end if
        if op == 47 then
            x = x / num;
        end if

        temp = x;

        printInt(x);
        println();
    wend
end function
